<h2>New General Practitioner</h2>

<%= render "form.html", changeset: @changeset,
                        action: gp_path(@conn, :create) %>

<%= link "Back", to: gp_path(@conn, :index), class: "btn btn-default btn" %>
