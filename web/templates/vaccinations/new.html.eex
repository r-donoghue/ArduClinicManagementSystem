<h2>New vaccinations</h2>

<%= render "form.html", changeset: @changeset,
                        action: vaccinations_path(@conn, :create) %>

<%= link "Back", to: vaccinations_path(@conn, :index) %>
