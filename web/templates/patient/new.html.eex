<h2>Patient Details</h2>

<%= render "form.html", changeset: @changeset, inactivity: @inactivity, doctors: @doctors, gps: @gps, pharms: @pharms,
                        action: patient_path(@conn, :create) %>

<%= link "Back", to: patient_path(@conn, :index), class: "btn btn-default btn" %>
