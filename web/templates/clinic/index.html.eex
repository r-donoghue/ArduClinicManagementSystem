<h2>Listing clinics</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for clinic <- @clinics do %>
    <tr>
      <td><%= clinic.name %></td>
      <td><%= clinic.address %></td>

      <td class="text-right">
        <%= link "Show", to: clinic_path(@conn, :show, clinic), class: "btn btn-primary" %>
        <%= link "Edit", to: clinic_path(@conn, :edit, clinic), class: "btn btn-primary" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "Add clinic", class: "btn btn-primary", to: clinic_path(@conn, :new) %>
