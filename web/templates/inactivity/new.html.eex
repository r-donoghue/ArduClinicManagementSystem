<h2>Add patient inactivity reason</h2>

<%= render "form.html", changeset: @changeset,
                        action: inactivity_path(@conn, :create) %>

<%= link "Back", to: inactivity_path(@conn, :index), class: "btn btn-default btn"  %>
